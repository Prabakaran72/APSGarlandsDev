<div class="row">
    <div class="col-md-8">
        {{-- {{ Form::text('id', trans('recurring::attributes.name'), $errors, $recurring, ['required' => true]) }}

        {{ Form::text('recurring_order_date_count', trans('recurring::attributes.name'), $errors, $recurring, ['required' => true]) }}

        {{ Form::textarea('recurring_order_dates', trans('recurring::attributes.comment'), $errors, $recurring, ['required' => true]) }}

        {{ Form::checkbox('is_active', trans('recurring::attributes.is_active'), trans('recurring::recurrings.form.enable_the_recurring'), $errors, $recurring) }} --}}
    </div>
</div>
